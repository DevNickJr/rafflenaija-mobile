import { StyleSheet } from "react-native";

const authStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    },
    form: {
        padding: 30,
        marginTop: 50,
    },
})

export default authStyle;